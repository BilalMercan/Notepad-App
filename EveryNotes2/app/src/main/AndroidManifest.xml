<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a.everynotes">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ikon_onplan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ikon_onplan"
        android:supportsRtl="true"
        android:theme="@style/Theme.EveryNotes">
        <activity android:name=".Hakkinda" />
        <activity android:name=".Ayarlar" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".Mainmenu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddNotes"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>